#include <iostream>
#include <iomanip>
#include <cmath>
using namespace std;
int main(){
floar x,y,z;
cin >> x >> y >> z;
if (x<0) x =x*x;
if (y<0) y = y*y;
if (z<0) z = z*z;
cout << setprecision(2) << fixed;
cout x << " " << y << " " << z;
}
